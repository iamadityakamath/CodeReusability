# MongoDB Python Template

This template provides a minimal Python setup for MongoDB with common operations:
- Connect to MongoDB
- Insert documents
- Read documents
- Update document values

## Setup

1. Install dependencies:

```bash
pip install -r requirements.txt
```

2. Configure environment variables using `.env.example`.

3. Run:

```bash
python db.py
```
